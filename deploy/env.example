# PwnHub Configuration
HUB_HOST=localhost

# Optional: Override API port
# API_PORT=5000

# Optional: Override Web UI port
# WEB_PORT=8080

# Retention Policy
RETENTION_ENABLED=true
RETENTION_DAYS=90
RETENTION_MAX_GB_PER_DEVICE=10
RETENTION_INTERVAL_HOURS=24

